package Connection;

public enum MessageType {
    REQUEST_NAME_USER,
    TEXT_MESSAGE,
    USER_PASSWORD,
    NAME_ACCEPTED,
    USER_NAME,
    NAME_USED,
    USER_ADDED,
    DISABLE_USER,
    REMOVED_USER;
}